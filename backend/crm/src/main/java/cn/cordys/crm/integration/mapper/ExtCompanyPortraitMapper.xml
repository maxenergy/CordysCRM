<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="cn.cordys.crm.integration.mapper.ExtCompanyPortraitMapper">

    <select id="selectByCustomerId" resultType="cn.cordys.crm.integration.domain.CompanyPortrait">
        SELECT id, customer_id, portrait, opportunities, risks, public_opinion,
               model, version, source, generated_at, organization_id,
               create_time, update_time, create_user, update_user
        FROM company_portrait
        WHERE customer_id = #{customerId}
    </select>

    <delete id="deleteByCustomerId">
        DELETE FROM company_portrait WHERE customer_id = #{customerId}
    </delete>

</mapper>
